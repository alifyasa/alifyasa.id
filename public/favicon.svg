<?xml version="1.0" ?>
<svg class="feather feather-user" fill="none" height="24" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" width="24" xmlns="http://www.w3.org/2000/svg">
    <defs>
        <style>
            :root {
                --svg-path-color: #000; /* Color for the path stroke */
                --svg-circle-stroke-color: #000; /* Color for the circle stroke */
            }
            @media (prefers-color-scheme: dark) {
                :root {
                    --svg-path-color: #FFF; /* Color for the path stroke in dark mode */
                    --svg-circle-stroke-color: #FFF; /* Color for the circle stroke in dark mode */
                }
            }
            path { stroke: var(--svg-path-color); }
            circle { fill: none; stroke: var(--svg-circle-stroke-color); }
        </style>
    </defs>
    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
    <circle cx="12" cy="7" r="4"/>
</svg>
